---
# install_glances.yml — Install Glances monitoring with web UI.
#
# Usage:
#   ansible-playbook playbooks/install_glances.yml

- name: Install Glances
  hosts: glances_capable
  become: true
  gather_facts: false

  roles:
    - role: glances
